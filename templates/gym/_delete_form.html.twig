<form method="post" action="{{ path('app_gym_delete', {'idg': gym.idg}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gym.idg) }}">
    <button class="btn">Delete</button>
</form>
