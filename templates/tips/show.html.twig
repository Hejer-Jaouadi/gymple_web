{% extends 'base.html.twig' %}

{% block title %}Tips{% endblock %}

{% block body %}
    <h1>Tips</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tip.id }}</td>
            </tr>
            <tr>
                <th>Caption</th>
                <td>{{ tip.caption }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ tip.category }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ tip.text }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tips_index') }}">back to list</a>

    <a href="{{ path('app_tips_edit', {'id': tip.id}) }}">edit</a>

    {{ include('tips/_delete_form.html.twig') }}
{% endblock %}
