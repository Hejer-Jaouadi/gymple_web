<form method="post" action="{{ path('app_room_delete', {'idr': room.idr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room.idr) }}">
    <button class="btn">Delete</button>
</form>
